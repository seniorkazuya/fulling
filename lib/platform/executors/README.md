# Executors

`lib/platform/executors/` contains effectful operations that run only after orchestration has decided work is ready.

Executors perform work. They should not decide whether work ought to run.
