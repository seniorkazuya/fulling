# Persistence

`lib/platform/persistence/` contains database-facing state persistence for the control plane.

This layer is responsible for durable writes, row claiming, lock management, and persisted state transitions.
